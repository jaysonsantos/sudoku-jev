# OpenRouter API key. The backend sends every decision request with it.
OPENROUTER_API_KEY=sk-or-v1-...
# OpenRouter model id of the decision model. Default: typesafe/jev-1.13
JEV_MODEL=typesafe/jev-1.13
# TCP port of the backend. Default: 8080
PORT=8080
